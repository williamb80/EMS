@model IEnumerable<EMS.Mvc.ViewModels.Registration.ChurchViewModel>

@{
    ViewBag.Title = "Index";
}

<h1>@Html.Raw(EMS.Resource.Registration.ChurchResource.Church)</h1>

<p>
    @Html.ActionLink(EMS.Framework.Resource.ButtonsResource.New, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Initials)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BirthDate)
        </th>
        <th>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Initials)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthDate)
            </td>
            <td>
                <a href="#" id="btnDetail" data-value="@item.Id" class="btn btn-default btnDetail" data-loading-text="@Html.Raw(EMS.Framework.Resource.ButtonsResource.Loading)...">@Html.Raw(EMS.Framework.Resource.ButtonsResource.View)</a>
                <a href="#" id="btnEdit" data-value="@item.Id" class="btn btn-default btnEdit" data-loading-text="@Html.Raw(EMS.Framework.Resource.ButtonsResource.Loading)...">@Html.Raw(EMS.Framework.Resource.ButtonsResource.Edit)</a>
            </td>
        </tr>
    }

</table>

<div class="modal fade" tabindex="-1" role="dialog" id="Modal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">@Html.Raw(EMS.Resource.Registration.ChurchResource.Church)</h4>
            </div>
            <div class="modal-body">
                <div id="popupContent">

                </div>
            </div>
            <div class="modal-footer">
                <a href="#" data-dismiss="modal">@Html.Raw(EMS.Framework.Resource.ButtonsResource.Close)</a>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div><!-- /.modal -->


@section scripts {
    <script type="text/javascript">

        $(document).ready(

            $('.btnDetail').on('click', function () {

                ConfigureButtonsToOpenModal($(this), "/Church/Detail/", 'Modal')

            })

        );

        $(document).ready(

            $('.btnEdit').on('click', function () {
                ConfigureButtonsToOpenModal($(this), "/Church/Edit/", 'Modal')
            })

        );

        //$(document).ready(function () {
        //    $(".btnDetail").click(function () {
        //        var id = $(this).data("value");

        //        var $btn = $(this).button('loading')

        //        $("#popupContent").load("/Church/Detail/" + id, function () {
        //            $('#Modal').modal("show");
        //        });

        //        $btn.button('reset')

        //    });
        //});

        //$(".btnEdit").click(function () {
        //    var id = $(this).data("value");

        //    $("#popupContent").load("/Church/Edit/" + id, function () {
        //        $('#Modal').modal("show");
        //    });
        //});




    </script>

}